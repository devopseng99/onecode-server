apiVersion: v1
kind: Service
metadata:
  name: {{ include "onecode-server.fullname" . }}
  labels:
    {{- include "onecode-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onecode-server.selectorLabels" . | nindent 4 }}
